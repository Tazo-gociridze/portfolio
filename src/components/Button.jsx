

export const Button = ({children}) => {
    return <button className="general-bg-color border-none px-[33px] hover:scale-[1.03] transition-[0.3s]
    py-[13px] rounded-[30px] text-[white]">{children}</button>
}